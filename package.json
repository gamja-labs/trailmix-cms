{
  "name": "trailmixcms",
  "version": "0.12.4",
  "private": true,
  "packageManager": "yarn@4.17.0",
  "scripts": {
    "test": "yarn workspaces foreach --topological --all --exclude core-web --exclude cms-web run test",
    "build": "yarn workspaces foreach --topological --all --exclude core-web --exclude cms-web run build",
    "ci": "yarn build",
    "test:e2e": "./test/e2e/run.sh",
    "clean:all": "yarn workspaces foreach --all run clean"
  },
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "dependencies": {
    "typescript": "6.0.3"
  },
  "devDependencies": {
    "@yarnpkg/types": "^4.0.1"
  }
}
