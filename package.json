{
  "name": "trailmixcms",
  "version": "0.4.1",
  "private": true,
  "packageManager": "yarn@4.12.0",
  "scripts": {
    "build": "yarn workspaces foreach --topological --all --exclude cms-api --exclude simple-todo-api --exclude cms-web run build",
    "ci": "yarn build"
  },
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "dependencies": {
    "typescript": "5.9.3"
  },
  "devDependencies": {
    "@yarnpkg/types": "^4.0.1"
  }
}
